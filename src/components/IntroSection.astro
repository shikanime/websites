---
import { getImage } from "astro:assets";
import IntroSectionBackground from "../assets/intro-section-background.webp";

const optimizedIntroSectionBackground = await getImage({
  src: IntroSectionBackground,
});
---

<section
  id="dreamseeker"
  class="flex min-h-screen w-full flex-col items-center justify-center bg-base-100 bg-center"
  style={{ backgroundImage: `url('${optimizedIntroSectionBackground.src}')` }}
>
  <div class="flex max-w-prose flex-col gap-y-4 rounded p-4 backdrop-blur">
    <h2 class="text-3xl font-extrabold">✨ Our Dream</h2>
    <p class="text-xl">
      We are committed to cultivating an experience, a universe and a movement
      that embodies the very essence of innovation for humanity. Our journey is
      fueled by a deep reverence for the past and an unwavering commitment to
      shaping the future. Together, for all the dreamers who aspire the world,
      we are on a quest that will shape the way we meaningfully engage with
      technology forever.
    </p>
  </div>
</section>
